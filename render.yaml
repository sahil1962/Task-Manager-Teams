services:
  - type: web
    name: task-manager
    env: node
    buildCommand: npm install && npm run build
    startCommand: serve -s build -l $PORT --single
    envVars:
      - key: PUBLIC_URL
        value: "/"